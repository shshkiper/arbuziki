# Drink with Book ☕📚

Мобильное приложение для кафе-книжного магазина с системой заказов, клубами по интересам и программой лояльности.

## 📱 Описание

**Drink with Book** - это современное мобильное приложение, которое объединяет в себе функции заказа кофе и чая, покупки книг, участие в тематических клубах и программу лояльности. Приложение создано для создания уютной атмосферы кафе-книжного магазина, где посетители могут не только заказать напитки, но и присоединиться к сообществу единомышленников.

## 🚀 Установка и запуск

### Требования
- Flutter SDK 3.35.0+
- Dart SDK 3.7.2+
- Android Studio / VS Code
- Аккаунт Supabase (для бэкенда)

### Установка

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd drinkwithbook
```

2. **Установка зависимостей**
```bash
flutter pub get
```

3. **Настройка переменных окружения**
Создайте файл `.env` в корне проекта:
```env
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
```

4. **Генерация кода**
```bash
flutter packages pub run build_runner build
```

5. **Запуск приложения**
```bash
flutter run
```

## 🏗️ Архитектура

### Структура проекта
```
lib/
├── data/
│   ├── models/          # Модели данных (Freezed)
│   ├── providers/       # Riverpod провайдеры
│   └── services/        # Бизнес-логика и API
├── presentation/
│   ├── pages/          # Экраны приложения
│   ├── widgets/        # Переиспользуемые виджеты
│   └── theme/          # Темизация
└── main.dart           # Точка входа
```

### Технологический стек
- **Flutter** - UI фреймворк
- **Riverpod** - Управление состоянием
- **Supabase** - Backend-as-a-Service
- **GoRouter** - Навигация
- **Freezed** - Генерация immutable классов
- **Flutter Animate** - Анимации

### Паттерны проектирования
- **MVVM** - Разделение UI и бизнес-логики
- **Provider Pattern** - Управление состоянием
- **Repository Pattern** - Абстракция данных
- **Service Layer** - Бизнес-логика

## 🎯 Основные функции

### 1. Система аутентификации
**Пользовательские истории:**
- **Регистрация**: Пользователь вводит email, пароль и имя → получает подтверждение на email → аккаунт активируется
- **Вход**: Пользователь вводит email и пароль → получает доступ к приложению
- **Автоматический вход**: При повторном запуске приложения пользователь автоматически входит в систему

### 2. Меню и заказы
**Пользовательские истории:**
- **Просмотр меню**: Пользователь открывает вкладку "Меню" → видит категории (Кофе, Чай, Пончики, Книги) → выбирает категорию → просматривает товары с ценами и описаниями
- **Добавление в корзину**: Пользователь нажимает на товар → видит детали → нажимает "В корзину" → товар добавляется в корзину
- **Оформление заказа**: Пользователь открывает корзину → проверяет товары → выбирает способ получения (самовывоз/доставка) → добавляет комментарий → подтверждает заказ
- **Отслеживание заказа**: После оформления заказ появляется на главной странице → статус обновляется каждые 30 секунд (Ожидает → Готовится → Готов → Завершен)

### 3. Программа лояльности
**Пользовательские истории:**
- **Накопление баллов**: За каждые 10 рублей покупки начисляется 1 балл → баллы отображаются в профиле
- **Уровни лояльности**: При накоплении баллов уровень повышается (Bronze → Silver → Gold → Platinum)
- **Использование баллов**: При оформлении заказа пользователь может потратить баллы на скидку
- **История транзакций**: Пользователь может посмотреть историю начисления и списания баллов

### 4. Клубы по интересам
**Пользовательские истории:**
- **Просмотр клубов**: Пользователь открывает вкладку "Клубы" → видит список клубов (Кофейные гурманы, Книжный клуб, Чайная церемония)
- **Присоединение к клубу**: Пользователь выбирает клуб → нажимает "Присоединиться" → становится участником
- **Участие в событиях**: Пользователь видит предстоящие события клуба → записывается на событие
- **Общение в чате**: Пользователь может писать сообщения в общий чат или чат клуба

### 5. Профиль и настройки
**Пользовательские истории:**
- **Просмотр профиля**: Пользователь видит свои данные, уровень лояльности, количество баллов
- **История заказов**: Пользователь может посмотреть все свои заказы с деталями и статусами
- **Настройки**: Пользователь может выбрать тему (светлая/темная/системная), изменить язык
- **Выход из аккаунта**: Пользователь может выйти из системы

## 🔄 Алгоритмы и логика

### Система заказов
```dart
// Алгоритм создания заказа
1. Валидация корзины (не пустая)
2. Создание заказа в БД со статусом 'pending'
3. Создание позиций заказа (order_items)
4. Начисление баллов лояльности (1 балл за 10₽)
5. Запуск симуляции статуса заказа
6. Отображение заказа на главной странице
```

### Симуляция статуса заказа
```dart
// Алгоритм обновления статуса
Timer.periodic(Duration(seconds: 30), () {
  if (время_прошло < 30_сек) статус = 'pending'
  else if (время_прошло < 90_сек) статус = 'preparing' 
  else статус = 'ready'
})
```

### Программа лояльности
```dart
// Алгоритм начисления баллов
баллы_к_начислению = округлить(сумма_заказа / 10)
новый_уровень = определить_уровень(текущие_баллы + баллы_к_начислению)
обновить_профиль(новые_баллы, новый_уровень)
```

### Управление состоянием
```dart
// Паттерн Provider для состояния корзины
class CartNotifier extends StateNotifier<List<CartItem>> {
  void addItem(ProductModel product) {
    // Логика добавления товара
  }
  
  void removeItem(String productId) {
    // Логика удаления товара
  }
}
```

## 🎨 UI/UX особенности

### Дизайн-система
- **Цветовая схема**: Адаптивные цвета для светлой и темной темы
- **Типографика**: Кастомный шрифт Graphik
- **Анимации**: Плавные переходы с flutter_animate
- **Модальные окна**: Bottom sheets для форм и деталей

### Навигация
- **Bottom Navigation**: 4 основные вкладки (Главная, Меню, Клубы, Профиль)
- **PageView**: Плавная прокрутка между экранами
- **GoRouter**: Декларативная навигация

### Адаптивность
- **Responsive Design**: Адаптация под разные размеры экранов
- **Theme Support**: Поддержка светлой, темной и системной темы
- **Accessibility**: Поддержка accessibility features

## 🔧 Конфигурация

### Режимы работы
Приложение поддерживает два режима:
- **Production**: Подключение к Supabase
- **Development**: Mock-сервисы для тестирования

### Переменные окружения
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
```

### Настройка темы
```dart
// Переключение в main.dart
final themeProvider = StateNotifierProvider<ThemeNotifier, ThemeMode>((ref) {
  return ThemeNotifier();
});
```

## 📊 Модели данных

### Основные сущности
- **UserModel**: Профиль пользователя с баллами лояльности
- **ProductModel**: Товары (кофе, чай, десерты, книги)
- **OrderModel**: Заказы с позициями и статусами
- **ClubModel**: Клубы по интересам
- **LoyaltyTransaction**: Транзакции программы лояльности

### Связи между моделями
```
User 1:N Order
Order 1:N OrderItem
User 1:N LoyaltyTransaction
Club 1:N ClubEvent
Club 1:N ClubPost
```

## 🚀 Развертывание

### Android
```bash
flutter build apk --release
flutter build appbundle --release
```

### iOS
```bash
flutter build ios --release
```

### Web
```bash
flutter build web --release
```

## 🧪 Тестирование

### Запуск тестов
```bash
flutter test
```

### Линтинг
```bash
flutter analyze
```

Этот проект является демонстрационным и создан в образовательных целях.

---

**Drink with Book** - где каждая чашка кофе и каждая книга создают истории! ☕📚✨
